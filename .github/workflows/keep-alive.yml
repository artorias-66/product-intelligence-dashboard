name: Render Keep-Alive Ping
on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Render Backend
        run: curl -s https://product-intelligence-api-dkuo.onrender.com/api/health > /dev/null
